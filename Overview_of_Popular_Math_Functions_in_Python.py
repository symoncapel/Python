import math

loss = -20.25
product_cost = 89.99

print(abs(loss))
print(math.floor(product_cost)) #shows your intintion instead of using .int
print(math.ceil(product_cost))
print(abs(math.floor(loss)))
print(round(product_cost)) #simply rounds to the nearest whole number
print(math.sqrt(product_cost)) #square root
print(math.pow(5, 2))

#some very common and popular functions that include are included in the math library and in the standard number class in Python.